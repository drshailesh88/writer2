# ─── Clerk (Authentication) ───
# Required: Get from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
CLERK_SECRET_KEY=sk_test_your_key_here
CLERK_JWT_ISSUER_DOMAIN=https://your-domain.clerk.accounts.dev
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# ─── Convex (Database) ───
# Required: Get from https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# ─── Convex Action Secret (Required) ───
# Shared secret between Next.js and Convex for server-side action calls
# Must be identical in both Next.js env and Convex deployment env vars
# Generate with: openssl rand -hex 32
CONVEX_ACTION_SECRET=

# ─── Paper Search APIs (Optional — enables higher rate limits) ───
NCBI_API_KEY=
SEMANTIC_SCHOLAR_API_KEY=
OPENALEX_EMAIL=

# ─── Copyleaks (Plagiarism + AI Detection) ───
# Required for plagiarism/AI detection features
COPYLEAKS_EMAIL=
COPYLEAKS_API_KEY=
COPYLEAKS_WEBHOOK_SECRET=         # Required — separate from API key, used for webhook HMAC verification
COPYLEAKS_WEBHOOK_BASE_URL=http://localhost:3000

# ─── Razorpay (Payments) ───
# Required for subscription features
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
NEXT_PUBLIC_RAZORPAY_KEY_ID=
RAZORPAY_BASIC_PLAN_ID=
RAZORPAY_PRO_PLAN_ID=

# ─── Sentry (Error Monitoring) ───
# Optional: Get from https://sentry.io
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# ─── PostHog (Analytics) ───
# Optional: Get from https://posthog.com
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ─── Upstash Redis (Rate Limiting) ───
# Required for production — distributed rate limiting across serverless instances
# Get from https://console.upstash.com — create a Redis database
# In development, rate limiting falls back to in-memory counters
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ─── AI / LLM ───
# Required for Draft Mode, Learn Mode, Deep Research
# GLM_API_KEY=  (set in Mastra config or here)
